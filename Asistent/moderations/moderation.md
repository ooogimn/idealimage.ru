Предлагаю привести модерацию к управляемой и расширяемой архитектуре. Сейчас всё «зашито» в moderation_service.py, из-за чего мы не можем гибко включать или выключать отдельные проверки и подстраиваться под категории. Что сделать:
Вытянуть проверки в “конвейер”
Каждое правило (медиа, текст, SEO, FAQ и т.п.) оформить отдельным классом/функцией с единым интерфейсом: should_run, run, auto_fix.
Конвейер строим динамически на основании записей ModerationCriteria: читаем из базы и определяем, какие шаги активны, приоритеты, нужны ли автоисправления.
Это сразу даст прозрачность: можно включить/выключить правило, поменять порядок, настроить разные наборы для категорий и ролей.
Превратить ModerationCriteria в источник правды
Добавить в модель поле типа rule_code (строковый идентификатор) — например, FAQ_REQUIRED, WORD_COUNT, IMAGE_REQUIRED.
Разнести настройки: пороги (мин. количество слов), расписание, список разрешённых AI-операций.
Для каждой категории хранить матрицу правил (глобальные + overrides по категории). В конвейере обрабатывать сначала глобальные, затем конкретные overrides, чтобы проще управлять.
Строго разделить “проверку” и “исправление”
Любая проверка сначала возвращает status, message, details.
Если в настройке для конкретного правила разрешены автоисправления и роль автора это допускает — вызываем auto_fix и заново прогоняем.
Логировать отдельно: что именно было исправлено и какой ценой (штраф баллов, уменьшение рейтинга). Это нужно и для отчётности авторам.
Баллы и штрафы как часть системы мотивации
В ModerationCriteria храним “стоимость” нарушения; при автоисправлении списываем баллы (храним в AuthorBalance).
Это позволяет авторам видеть, где они теряют очки, и стимулирует их улучшать качество до отправки на модерацию.
Сценарии по ролям
Разделяем режимы: superuser, editor, author_with_ai, author_manual.
В настройках указываем, какие правила применяются и какие автоисправления разрешены для каждой роли.
Убираем жёсткую привязку к is_staff: определять режим по явной политике (например, таблица “Пользователь → роль модерации”).
FAQ и SEO как обязательная секция
В критериях указываем флаг “обязателен FAQ” и “обязателен SEO audit”.
Запускать отдельный шаг SEOChecklistRule, куда входят: FAQ, длина заголовка/описания, schema, внутренние ссылки, отправка в поисковики.
Если шаг не может выполнить автоисправление (например, FAQ не сгенерировался) — фиксируем красный статус и останавливаем конвейер.
Расширяемое логирование
Каждое правило пишет подробный отчёт в ModerationLog (JSON: что проверили, результат, auto-fix, время).
AuthorNotification получает короткое человеческое резюме (что не так, какие действия нужно сделать).
Делать сводный отчёт по статье: какие шаги пройдены, сколько баллов начислено/списано, сколько времени заняла модерация.
Доработка UI администратора
В панели модерации показывать текущий набор правил для выбранной категории, статусы “пройдено/провалено”, кнопки для ручного подтверждения/отклонения, историю попыток.
Для редакторов было бы удобно иметь “черновик” отчёта, чтобы видно было, какие auto-fix сделаны AI, а какие требуют человека.
План дальнейших шагов
Шаг 1: вынести проверки в отдельные классы и построить базовый конвейер.
Шаг 2: интегрировать ModerationCriteria (чтение настроек) и роли.
Шаг 3: обновить UI с отображением правил и результатов.
Шаг 4: добавить отчёты/статистику (сколько статей провалилось по каким причинам, среднее время модерации и т.д.).
Шаг 5: подключить auto-тasks для ретро-модерации старых статей (очередь с прогоном через тот же конвейер, отдельные критерии “ретро”).
Эта архитектура даст нам управляемость, гибкость и прозрачность. После её внедрения можно легко докручивать отдельные правила (например, новый SEO-чек или антиспам), просто добавляя новый класс и конфигурацию в админке.


============================================================================================================================================
Что сделано
Построен модуль Asistent/moderations/: модели (ModerationCriteria, ModerationLog, модели для комментариев), формы, представления, urls, шаблоны и сервисы вынесены из «общего» приложения.
Добавлены новые поля slug, description, rule_config для ModerationCriteria (миграция Asistent/migrations/0047_moderationcriteria_rule_config_fields.py) и обновлён UI управления наборами правил/логами.
Переписан сервис проверки статей на конвейер ArticleModerationPipeline с набором правил (MEDIA_REQUIRED, WORD_COUNT, FAQ_REQUIRED, SEO_ENRICHMENT и др.); старый модуль Asistent/services/moderation_service.py теперь лишь прокси.
Обновлены формы и тексты (новые шаблоны в Asistent/moderations/templates/moderations/…), обновлены Asistent/forms.py, Asistent/views.py, Asistent/urls.py, Asistent/ai_moderator.py.
Созданы вспомогательные функции helpers.py и реестр правил, переписаны шаблоны управления/создания критериев и списка модерированных комментариев.
Что проверить локально
python manage.py makemigrations (проверить, что миграция 0047 уже попадает в план) и python manage.py migrate.
python manage.py check.
Прогнать сценарий модерации (создание статьи, запуск задачи run_article_moderation_task) и убедиться, что новые шаблоны форм открываются.
На сервере после синхронизации файлов повторно выполнить миграцию и перезапустить приложение (touch tmp/restart.txt), затем проверить работу UI и конвейера.
Перед загрузкой на сервер
Загрузить изменённые файлы/папки:
Asistent/moderations/ (весь пакет)
Asistent/models.py, Asistent/forms.py, Asistent/views.py, Asistent/urls.py, Asistent/ai_moderator.py
Asistent/services/moderation_service.py
Новые шаблоны: Asistent/moderations/templates/moderations/...
Миграция Asistent/migrations/0047_moderationcriteria_rule_config_fields.py
После загрузки на сервер выполнить python manage.py migrate и при необходимости touch tmp/restart.txt, затем сверить работу сайта.
Тесты
Тесты не запускались (необходимо прогнать вручную при наличии набора).

+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
Схема модульного конструктора
1. Словари (каталоги) компонентов
Triggers — события, запускающие пайплайн. Примеры:
MANUAL_TRIGGER (ручной старт)
POST_PUBLISHED (переход статьи в «published»)
POST_SAVED_AS_DRAFT (сохранение черновика)
AI_ASSIST_REQUESTED (включили флаг «хочу помощь AI»)
SCHEDULE_TRIGGER (cron или выбранное расписание AI)
Steps — атомарные операции, которые можно комбинировать в Lego-цепочки. Каждый шаг: код, описание, схема параметров, метод run(context, config) возвращает статус + обновления контекста.
Примеры:
MEDIA_REQUIRED, WORD_COUNT_CHECK, FAQ_ENFORCER, SEO_METADATA_REFRESH, TELEGRAM_BROADCAST, ADS_AUTO_INSERT, DONATIONS_WEEKLY_REPORT.
Шаг может принимать «варианты действия» (ничего / исправить / вернуть в черновик).
Actions — завершающие реакции после основной цепочки (уведомить автора, команду, отправить webhook).
2. Структура пайплайна
AutomationPipeline:  ├─ name, slug, kind, is_active  ├─ triggers: [TriggerConfig]  ├─ steps:    [StepConfig {priority, enabled, parameters}]  ├─ actions:  [ActionConfig]  └─ metadata: произвольные настройки (например, промпт, связанный критерий)
Порядок шагов задаётся приоритетом (можно перетаскивать).
В каждом шаге задаём параметры: вариант поведения, промпт, пороги, флаг «автофикс» и т.д.
Если нужен переход к следующему шагу без действия — выбираем «ничего не делать».
3. Исполнение
Триггер передаёт payload (например {"post_id": 123}) в execute_pipeline.
Runner последовательно вызывает шаги:
Проверяет enabled, приоритет.
Запускает run(), получает отчёт и context_updates.
При необходимости добавляет фактические изменения (FAQ-секция, SEO-мета, перевести в draft).
После шагов выполняются действия (уведомления, вебхуки).
Результат сохраняется в PipelineRunLog (статус, время, ошибки).
4. Поведение шагов (пример FAQ)
Параметры: mode (ничего / генерировать / вернуть в черновик), prompt_id.
Логика:
Проверяем наличие блока FAQ.
Если mode == 'generate' и блока нет → вызываем генерацию, сохраняем результат.
Если mode == 'draft' → возвращаем статью в черновик, оставляем комментарий.
Возвращаем отчёт (добавлено N вопросов, статус).
Следующий шаг получает обновлённый контекст (например, faq_added=True), может принимать решение.
5. Универсальные сценарии
«Лего» из шагов позволяет строить разные пайплайны: модерация, SEO, рассылка, донаты.
Можно иметь несколько пайплайнов разных типов (kind=article, kind=distribution) и триггеры выбирать по ситуации.
Дополнительно в metadata храним ссылки на шаблон промпта, набор критериев, пресеты параметров.



🧩 СХЕМА МОДУЛЬНОЙ СИСТЕМЫ ПАЙПЛАЙНОВ
🔧 1. Основные компоненты системы
Компонент
Описание
Пайплайн (Pipeline)
Набор шагов, выполняемых последовательно или по условию. Может быть привязан к событию: новая статья, редактирование, ручной запуск, расписание.
Шаг (Step)
Атомарная операция: проверка, генерация, модерация, уведомление и т.д.
Триггер (Trigger)
Событие, запускающее пайплайн: статья создана, статья отправлена на модерацию, вручную, по расписанию.
Условие (Condition)
Логика перехода: если нет изображения → запустить генерацию, если < 500 слов → уведомить автора.
Действие (Action)
Что делает шаг: вызов функции, отправка уведомления, изменение статуса.
Промт-шаблон (Prompt Template)
Готовый промт, используемый в шагах (например, "сгенерируй FAQ", "улучши стиль").
Метаданные статьи
Дополнительная информация: word_count, has_image, seo_score, has_faq, tone, category.

🔄 2. Алгоритм выполнения пайплайна
1. Происходит ТРИГГЕР (например, "статья создана")
2. Система ищет активные пайплайны, привязанные к этому триггеру
3. Для каждого пайплайна:
   a. Создаётся контекст выполнения (Post, User, Metadata)
   b. Выполняются шаги по порядку
   c. На каждом шаге:
      - Проверяется УСЛОВИЕ (если есть)
      - Если условие выполняется → запускается ДЕЙСТВИЕ
      - Если действие завершено → обновляются метаданные
      - Если ошибка → логируется, можно прервать или продолжить
   d. После всех шагов — уведомление (опционально)


3. Типы шагов (Step Types)
Тип
Описание
Примеры
Проверка (Validation)
Проверяет состояние статьи
есть ли изображение?, достаточно ли слов?, есть ли FAQ?
Генерация (Generation)
Генерирует контент
создать изображение, сгенерировать FAQ, улучшить текст
Модерация (Moderation)
Проверка на соответствие правилам
проверка на токсичность, проверка на плагиат, проверка на рекламу
SEO-оптимизация
Работа с метатегами, ключевыми словами
добавить meta description, проверить H1, оптимизировать URL
Уведомление (Notification)
Оповещение автора или модератора
уведомить об ошибке, уведомить о готовности
Изменение статуса
Меняет статус статьи
черновик → на модерации, опубликовано
Вызов внешнего API
Интеграция с внешними сервисами
отправить в Google Search Console, загрузить в Telegram

🎯 4. Пример пайплайна: "Модерация новой статьи"
name: "Модерация новой статьи"
trigger: "post_created"
steps:
  - type: validation
    name: "Проверка на изображение"
    condition: "post.kartinka is None"
    action: "generate_image"
    prompt_template: "Создай изображение по теме статьи"
    on_success: "перейти к следующему шагу"
    on_fail: "уведомить автора"

  - type: validation
    name: "Проверка длины"
    condition: "post.word_count < 500"
    action: "notify_author"
    message: "Статья слишком короткая. Минимум 500 слов."
    on_fail: "остановить пайплайн"

  - type: moderation
    name: "Проверка на токсичность"
    action: "run_toxicity_check"
    on_fail: "отклонить статью, уведомить модератора"

  - type: generation
    name: "Генерация FAQ"
    condition: "not post.has_faq"
    action: "generate_faq_block"
    prompt_template: "Создай FAQ по теме статьи"

  - type: seo
    name: "SEO-оптимизация"
    action: "auto_seo_optimize"
    steps:
      - "проверить meta description"
      - "добавить H2/H3"
      - "оптимизировать ключевые слова"

  - type: notification
    name: "Уведомить автора"
    condition: "pipeline.success"
    action: "send_telegram_notification"
    message: "Ваша статья прошла модерацию и готова к публикации!"

  - type: status
    name: "Сменить статус"
    action: "set_status"
    value: "ready_for_publish"










