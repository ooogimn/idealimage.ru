# 🔧 ИСПРАВЛЕНИЯ - Высота баннера и рамки карточек

**Дата:** 23 октября 2025  
**Версия:** 3.1  
**Статус:** ✅ Исправлено

---

## 🐛 Исправленные проблемы

### 1. Высота баннера не работала ✅

**Проблема:**
- При изменении `banner_height` в админке высота баннера не менялась
- Использовалось `height: 100%` вместо конкретных пикселей

**Решение:**
- Изменено на конкретную высоту в пикселях: `height: {card_height}px`
- Добавлен `box-sizing: border-box` для корректных расчетов
- Вычисляется высота карточки: `card_height = banner_height - 20` (padding 10px × 2)

**Файл:** `advertising/templatetags/ad_tags.py`

**Изменения:**
```python
# Было:
height: 100%

# Стало:
height: {card_height}px
```

**Теперь работает:**
- Установите banner_height = 150px в админке
- Баннер будет именно 150px высотой!

---

### 2. Рамки карточек слишком толстые ✅

**Проблема:**
- Gap между карточками был 15px (слишком толстый)
- Padding контейнера был 15px

**Решение:**
- Gap уменьшен с 15px → **5px** (в 3 раза)
- Padding уменьшен с 15px → **10px**

**Файл:** `advertising/templatetags/ad_tags.py`

**Изменения:**
```css
/* Было: */
gap: 15px;
padding: 15px;

/* Стало: */
gap: 5px;
padding: 10px;
```

**Визуально:**

**Было:**
```
┌────────┐    ┌────────┐    ┌────────┐    ┌────────┐
│        │    │        │    │        │    │        │
└────────┘    └────────┘    └────────┘    └────────┘
  <─15px─>      <─15px─>      <─15px─>
```

**Стало:**
```
┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐
│        │ │        │ │        │ │        │
└────────┘ └────────┘ └────────┘ └────────┘
  <─5px->   <─5px->    <─5px->
```

Карточки теперь ближе друг к другу и выглядят компактнее!

---

## 📝 Детали изменений

### Файл: `advertising/templatetags/ad_tags.py`

#### Функция `render_card_content()` (строки 257-295):

**Изменения:**
1. Добавлена переменная `inner_height = card_height - 16`
2. Для изображений: `height:{card_height}px` вместо `height:100%`
3. Для видео: `height:{card_height}px` вместо `height:100%`
4. Для текста: `height:{card_height}px` вместо `height:100%`
5. Добавлен атрибут `playsinline` для видео (для iOS)

#### Функция `render_banner_html()` (строки 318-332):

**Изменения:**
1. `card_height = banner_height - 20` (вычитаем padding 10px × 2)
2. `gap:5px` вместо `gap:15px`
3. `padding:10px` вместо `padding:15px`
4. Добавлен `box-sizing:border-box`

### Файл: `advertising/management/commands/setup_my_campaign.py`

**Изменения (строки 144-147):**
- HTML контент для header/footer теперь пустой
- Карточки рендерятся динамически через поля card{N}_*

---

## 🧪 Тестирование

### Тест 1: Высота баннера

1. Открыть админку `/admin/advertising/adbanner/`
2. Выбрать "Баннер в шапке"
3. Установить "Высота баннера (px)": **150**
4. Сохранить
5. Открыть главную страницу
6. **Результат:** Header баннер теперь 150px высотой ✅

### Тест 2: Рамки карточек

1. Открыть главную страницу
2. Посмотреть на header баннер
3. **Результат:** Промежутки между карточками 5px (компактно) ✅

### Тест 3: Загрузка изображения в карточку

1. Админка → "Баннер в шапке"
2. Развернуть "Карточка 1"
3. Тип: Изображение
4. Загрузить JPG
5. Сохранить
6. **Результат:** Изображение отображается в карточке ✅

---

## 📏 Новые параметры

### Отступы:

| Элемент | Было | Стало |
|---------|------|-------|
| Gap (между карточками) | 15px | **5px** (в 3 раза меньше) |
| Padding (контейнера) | 15px | **10px** |
| Расчет высоты карточки | `height - 30` | `height - 20` |

### Высота:

- **Минимум:** 50px
- **Максимум:** 300px
- **По умолчанию (header/footer):** 120px
- **По умолчанию (остальные):** 100px
- **Применяется:** Конкретно в пикселях, а не в процентах

---

## ✅ Результат

**Исправлено:**
- ✅ Высота баннера теперь изменяется
- ✅ Рамки карточек в 3 раза тоньше (5px вместо 15px)
- ✅ Карточки рендерятся динамически из полей БД
- ✅ Изображения и видео загружаются и отображаются корректно

**Побочные улучшения:**
- ✅ Добавлен `playsinline` для видео (iOS совместимость)
- ✅ Добавлен `box-sizing: border-box` для точных расчетов
- ✅ Код стал чище (убран дублирующий HTML из команды)

---

## 🚀 Загрузка на сервер

### Обновленные файлы (3 шт.):

1. `advertising/templatetags/ad_tags.py` ✅
   - Функция render_card_content (высота в px)
   - Функция render_banner_html (gap 5px, padding 10px)

2. `advertising/management/commands/setup_my_campaign.py` ✅
   - Пустой HTML для header/footer

3. `advertising/migrations/0006_adbanner_card1_icon_...and_more.py` ✅
   - 24 новых поля (если еще не загружена)

### На сервере:

```bash
# Если миграция 0006 еще не применена
python manage.py migrate advertising

# Пересоздать баннеры
python manage.py setup_my_campaign

# Перезапустить
touch restart.txt
```

---

## 🎯 Проверка после загрузки

### Тест высоты:

1. Админка → Баннеры → "Баннер в шапке"
2. Установить "Высота баннера": 200
3. Сохранить
4. Открыть главную страницу
5. **Ожидается:** Header высотой 200px

### Тест рамок:

1. Открыть главную страницу
2. Посмотреть на промежутки между карточками
3. **Ожидается:** Тонкие рамки 5px (компактно)

### Тест загрузки контента:

1. Админка → Баннеры → "Баннер в футере"
2. Карточка 2 → Тип: Видео
3. Загрузить MP4 файл
4. Сохранить
5. Открыть главную страницу, прокрутить вниз
6. **Ожидается:** Видео воспроизводится в карточке 2

---

## 📊 Итоговые параметры

### Параметры карточек:

```css
Container:
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 5px;              ← В 3 РАЗА МЕНЬШЕ
  padding: 10px;         ← УМЕНЬШЕНО
  height: {banner_height}px;  ← РАБОТАЕТ!
  box-sizing: border-box;

Card:
  height: {card_height}px;  ← КОНКРЕТНАЯ ВЫСОТА!
  (где card_height = banner_height - 20)
```

---

## ✅ ГОТОВО!

Все исправления применены и протестированы локально! 🎉

**Изменения:**
- ✅ Высота работает корректно
- ✅ Рамки в 3 раза тоньше
- ✅ Код оптимизирован

**Готово к загрузке на сервер!**

---

*Дата: 23.10.2025*  
*Версия: 3.1 - Исправления*

