# ✅ ФИНАЛ - Чередование баннеров в статьях

**Дата:** 23 октября 2025  
**Версия:** 3.3 ФИНАЛ  
**Статус:** ✅ ВЫПОЛНЕНО

---

## 🎯 Что реализовано

### 1. Динамические fieldsets в админке ✅
**В настройках баннеров:**
- Для 3-карточных баннеров → показываются **только Карточка 1, 2, 3**
- Для 4-карточных баннеров → показываются **все 4 карточки**
- **Карточка 4 автоматически скрывается** для баннеров в статьях!

### 2. Чередование баннеров в статьях ✅
**Создано 2 новых AdPlace:**
- `in_post_middle_1` — В середине статьи 1 (для 1-й и 3-й статьи)
- `in_post_middle_2` — В середине статьи 2 (для 2-й статьи)

**Логика отображения:**
- Статья 1 (минимальные просмотры) → **Баннер 1**
- Статья 2 → **Баннер 2**
- Статья 3 → **Баннер 1** (повторяется)
- Основная статья → старый баннер `in_post_middle`

---

## 📊 Итоговая конфигурация баннеров

### Всего AdPlace: 12 (было 10)
### Всего баннеров: 12 (было 10)

**С 4 карточками (2 шт.):**
1. `header_banner` — Баннер в шапке
2. `footer_banner` — Баннер в футере

**С 3 карточками (5 шт.):**
3. `before_article_content` — Перед текстом статьи
4. `in_post_middle` — В середине (основная статья)
5. `in_post_middle_1` — В середине 1 (1-я и 3-я статьи) ← **НОВЫЙ!**
6. `in_post_middle_2` — В середине 2 (2-я статья) ← **НОВЫЙ!**
7. `after_comments` — После комментариев

**Обычные (5 шт.):**
8. `sidebar_top`
9. `popup_modal`
10. `ticker_line`
11. `sidebar_after_author`
12. `sidebar_after_popular`

---

## 🎨 Визуальная схема на странице статьи

```
ОСНОВНАЯ СТАТЬЯ:
├── [Перед текстом - 3 карточки]
├── [Текст...]
├── [В середине - 3 карточки] ← in_post_middle (старый)
├── [Комментарии]
└── [После комментариев - 3 карточки]

СТАТЬЯ 1 (минимальные просмотры):
├── [Текст...]
└── [В середине - 3 карточки] ← in_post_middle_1 ✅

СТАТЬЯ 2:
├── [Текст...]
└── [В середине - 3 карточки] ← in_post_middle_2 ✅

СТАТЬЯ 3:
├── [Текст...]
└── [В середине - 3 карточки] ← in_post_middle_1 (повтор) ✅
```

**Результат:** Разные баннеры в разных статьях!

---

## 🔧 Изменения в коде

### 1. `advertising/admin.py`

**Добавлен метод:**
```python
def get_fieldsets(self, request, obj=None):
    """Динамические fieldsets в зависимости от места баннера"""
    # ...
    # Карточка 4 только для header_banner и footer_banner
    if obj and obj.place and obj.place.code in ['header_banner', 'footer_banner']:
        # Показываем карточку 4
```

**Результат:**
- Для "Перед текстом" → секции: Карточка 1, 2, 3 ✅
- Для "В середине 1" → секции: Карточка 1, 2, 3 ✅
- Для "В середине 2" → секции: Карточка 1, 2, 3 ✅
- Для "После комментариев" → секции: Карточка 1, 2, 3 ✅
- Для "Баннер в шапке" → секции: Карточка 1, 2, 3, **4** ✅
- Для "Баннер в футере" → секции: Карточка 1, 2, 3, **4** ✅

---

### 2. `templates/blog/partials/full_article.html`

**Добавлено чередование:**
```html
{% if article_index %}
    {% if article_index == 1 or article_index == 3 %}
        {% show_ad "in_post_middle_1" %}
    {% elif article_index == 2 %}
        {% show_ad "in_post_middle_2" %}
    {% endif %}
{% else %}
    {% show_ad "in_post_middle" %}
{% endif %}
```

---

### 3. `templates/blog/post_detail_tailwind.html`

**Передается индекс:**
```html
{% for article in low_view_posts %}
    {% include "blog/partials/full_article.html" with article=article article_index=forloop.counter %}
{% endfor %}
```

---

### 4. `advertising/templatetags/ad_tags.py`

**Обновлен список:**
```python
if place.code in ['in_post_middle', 'in_post_middle_1', 'in_post_middle_2', 'before_article_content', 'after_comments']:
    # Рендерим 3 карточки
```

---

### 5. `advertising/management/commands/setup_my_campaign.py`

**Обновлен список:**
```python
if place.code in ['header_banner', 'footer_banner', 'in_post_middle', 'in_post_middle_1', 'in_post_middle_2', 'before_article_content', 'after_comments']:
    # Создаем с пустым HTML
```

---

## 📦 Как настроить в админке

### Баннер "В середине статьи 1":

1. Админка → Баннеры → "В середине статьи 1"
2. Видим секции:
   - ✅ Карточка 1
   - ✅ Карточка 2
   - ✅ Карточка 3
   - ❌ Карточка 4 (НЕ отображается!)
3. Настроить 3 карточки:
   - Карточка 1: Изображение → Фото товара А
   - Карточка 2: Видео → Ролик про товар А
   - Карточка 3: Текст → ✨ "Акция" "Скидка"
4. Сохранить

### Баннер "В середине статьи 2":

1. Админка → Баннеры → "В середине статьи 2"
2. Настроить другой контент:
   - Карточка 1: Изображение → Фото товара Б
   - Карточка 2: Изображение → Фото товара В
   - Карточка 3: Текст → 💄 "Красота" "Новинка"
3. Сохранить

**Результат:**
- В 1-й и 3-й статьях → баннер с товаром А
- Во 2-й статье → баннер с товарами Б и В
- **Разные баннеры!** ✅

---

## 🧪 Тестирование

### Как проверить:

1. Открыть любую статью: `/blog/post/любая/`
2. Прокрутить вниз до раздела "Откройте для себя ещё..."
3. Увидеть 3 статьи с минимальными просмотрами
4. **В 1-й статье** — баннер "В середине статьи 1"
5. **Во 2-й статье** — баннер "В середине статьи 2"
6. **В 3-й статье** — баннер "В середине статьи 1" (повтор)

**Все баннеры разные!** ✅

---

## 📁 Измененные файлы (5 шт.)

1. ✅ `advertising/admin.py`
   - Метод get_fieldsets() для динамических секций

2. ✅ `advertising/templatetags/ad_tags.py`
   - Добавлено in_post_middle_1 и in_post_middle_2

3. ✅ `advertising/management/commands/setup_my_campaign.py`
   - Добавлено 2 новых места в список

4. ✅ `templates/blog/partials/full_article.html`
   - Логика чередования баннеров

5. ✅ `templates/blog/post_detail_tailwind.html`
   - Передача article_index

**+ 2 новых AdPlace в БД** (созданы через shell)

---

## 🚀 Загрузка на сервер

### Файлы для загрузки (5 шт.):

```
advertising/admin.py
advertising/templatetags/ad_tags.py
advertising/management/commands/setup_my_campaign.py
templates/blog/partials/full_article.html
templates/blog/post_detail_tailwind.html
```

### Команды на сервере:

```bash
# 1. Создать 2 новых AdPlace
python manage.py shell -c "from advertising.models import AdPlace; AdPlace.objects.get_or_create(code='in_post_middle_1', defaults={'name': 'В середине статьи 1', 'description': 'Баннер для 1-й и 3-й статьи', 'placement_type': 'banner', 'recommended_size': '728x90', 'is_active': True, 'position_order': 8}); AdPlace.objects.get_or_create(code='in_post_middle_2', defaults={'name': 'В середине статьи 2', 'description': 'Баннер для 2-й статьи', 'placement_type': 'banner', 'recommended_size': '728x90', 'is_active': True, 'position_order': 9}); print('OK')"

# 2. Пересоздать баннеры
python manage.py setup_my_campaign

# 3. Перезапустить
touch restart.txt
```

**Миграций НЕ нужно** — используем существующие поля!

---

## ✅ Итоговая статистика

**AdPlace:** 12 (было 10, +2 новых)
**Баннеров:** 12 (было 10, +2 новых)

**Распределение:**
- 4 карточки: 2 баннера (header, footer)
- 3 карточки: 5 баннеров (before, middle×3, after)
- Обычные: 5 баннеров

**В админке:**
- Для 3-карточных → секции 1, 2, 3 (БЕЗ 4-й!) ✅
- Для 4-карточных → секции 1, 2, 3, 4 ✅

**На сайте:**
- Разные баннеры в разных статьях! ✅

---

## 🎉 ГОТОВО!

Все нюансы исправлены и реализованы!

1. ✅ Карточка 4 убрана из настроек 3-карточных баннеров
2. ✅ Баннеры чередуются (баннер 1 в 1-й и 3-й статье, баннер 2 во 2-й)
3. ✅ Всё остальное один в один (gap 5px, высота работает, БЕЗ border)

**Готово к загрузке на сервер!** 🚀

---

*Дата: 23.10.2025*  
*Версия: 3.3 ФИНАЛ - Чередование баннеров*

